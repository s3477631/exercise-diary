<%= form_with(model: exercise_templates) do |form| %>
  <% if exercise_templates.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(exercise_template.errors.count, "error") %> prohibited this friend from being saved:</h2>

      <ul>
        <% exercise_template.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="input-group mb-3">
    <%=  form.text_field :exercise_name, class: "form-control", placeholder: "Exercise Name" %>
  </div>

 <div class="input-group mb-3">
    <%= form.number_field :weight, class: "form-control", placeholder: "Weight" %>
  </div>

 <div class="input-group mb-3">
    <%= form.number_field :reps, class: "form-control", placeholder: "Reps" %>
  </div>

 <div class="input-group mb-3">
    <%= form.number_field :sets, class: "form-control", placeholder: "Sets" %>
  </div>
    <div class="input-group mb-3">
      <%= form.number_field :user_id,
      class: "form-control", value: current_user.id, type: :hidden%>
    </div>


  <div class="d-flex justify-content-end">
    <%= form.submit class:'btn btn-success' %>
  </div>
<% end %>
